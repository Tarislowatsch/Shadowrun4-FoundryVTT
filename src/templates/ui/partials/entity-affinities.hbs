{{!-- buttons: [{action, icon, label, tooltip?}] --}}
<details class="inventory-section collapsible-section" open>
  <summary class="collapsible-header">
    <i class="fas fa-chevron-right collapse-icon"></i>
    <h3>{{localize sectionLabel}}</h3>
    {{#each buttons as |btn|}}
    <button
      type="button"
      class="action-button"
      data-action="{{btn.action}}"
      {{#if btn.tooltip}}data-tooltip="{{localize btn.tooltip}}"{{/if}}
    >
      <i class="{{btn.icon}}"></i>
      {{localize btn.label}}
    </button>
    {{/each}}
  </summary>
  <table class="item-stats-table">
    <tbody>
      {{#each categories as |cat|}}
      <tr>
        <td>{{cat.label}}</td>
        <td>
          {{#if cat.options}}
          <select
            id="{{../idPrefix}}-{{cat.key}}"
            name="{{../namePrefix}}.{{cat.key}}"
            data-dtype="String"
          >
            <option value="">—</option>
            {{#each cat.options as |opt|}}
            <option value="{{opt}}" {{#if (eq opt cat.value)}}selected{{/if}}>{{opt}}</option>
            {{/each}}
          </select>
          {{else}}
          <input
            type="text"
            id="{{../idPrefix}}-{{cat.key}}"
            name="{{../namePrefix}}.{{cat.key}}"
            value="{{cat.value}}"
          />
          {{/if}}
          {{#if ../createAction}}{{#if @root.editMode}}
          <button
            type="button"
            class="action-button"
            data-action="{{../createAction}}"
            data-type-key="{{cat.key}}"
            data-tooltip="{{localize 'sr4.ui.createTemplate'}}"
          ><i class="fas fa-plus"></i></button>
          {{/if}}{{/if}}
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</details>
