{{#if edgeAvailable}}
<div class="form-group">
  <label for="edge">{{localize "sr4.roll.useEdge"}}</label>
  <input type="checkbox" id="edge" name="edge" />
  <span class="edge-count"
    >({{localize "sr4.stats.CURRENTEDGE"}}: {{ currentEdge }})</span
  >
</div>
{{/if}}
